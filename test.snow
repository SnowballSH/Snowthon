let a = 1.3;  # define variable "a" to float 1.3 ;

func out(str, s2) {
    cout << str |
    cout << s2 |
    cout << "I am a string"
};

if (a==1.3) {
    let a = a+1 |
    out("a func", "me")
};

cout << a;  # output a which is 1.3 + 1